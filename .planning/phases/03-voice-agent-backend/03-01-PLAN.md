---
phase: 03-voice-agent-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/knowledge_loader.py
  - backend/tools.py
  - backend/requirements.txt
  - tests/conftest.py
  - tests/test_knowledge_loader.py
  - tests/test_tools.py
  - tests/test_system_instruction.py
autonomous: true
requirements: [CALL-02, CALL-03, CALL-04, CALL-05, CALL-06, CALL-10]

must_haves:
  truths:
    - "All 5 Firestore KB documents are loaded and concatenated into the system instruction"
    - "System instruction includes AI disclosure text matching CONTEXT.md locked wording"
    - "System instruction includes qualification decision tree from conversation-sequence PDF"
    - "System instruction includes both programme details (Cloud Security and SRE & Platform Engineering)"
    - "update_lead_profile tool captures role, experience_level, cloud_background, motivation"
    - "determine_call_outcome tool validates outcome is COMMITTED, FOLLOW_UP, or DECLINED"
    - "determine_call_outcome tool captures follow_up_preference for FOLLOW_UP outcomes"
  artifacts:
    - path: "backend/knowledge_loader.py"
      provides: "Firestore KB pre-loader and system instruction builder"
      exports: ["load_knowledge_base", "build_system_instruction"]
    - path: "backend/tools.py"
      provides: "ADK function tools for Supabase side-effects"
      exports: ["update_lead_profile", "determine_call_outcome"]
    - path: "tests/conftest.py"
      provides: "Shared test fixtures for mock Firestore, Supabase, ADK"
    - path: "tests/test_knowledge_loader.py"
      provides: "Tests for KB loading and system instruction building"
    - path: "tests/test_tools.py"
      provides: "Tests for tool functions"
    - path: "tests/test_system_instruction.py"
      provides: "Tests for system instruction content requirements"
  key_links:
    - from: "backend/knowledge_loader.py"
      to: "Firestore knowledge_base collection"
      via: "google-cloud-firestore AsyncClient"
      pattern: "collection.*knowledge_base.*document"
    - from: "backend/tools.py"
      to: "Supabase REST API"
      via: "httpx async PATCH/POST with Content-Profile: sales_agent"
      pattern: "httpx.*rest/v1/(leads|call_logs)"
---

<objective>
Build the foundation modules for Sarah's voice agent: the knowledge base loader that fetches all 5 Firestore PDFs and builds Sarah's complete system instruction (persona + KB + conversation rules), and the ADK function tools that handle Supabase side-effects during calls (updating lead profiles, determining call outcomes). Also scaffold the pytest test suite covering CALL-02 through CALL-06, CALL-10.

Purpose: These are the "contracts" -- every other Phase 3 file depends on the system instruction builder and tool functions. Building and testing them first eliminates the scavenger-hunt anti-pattern.
Output: knowledge_loader.py, tools.py (rewritten from stub), updated requirements.txt, full test suite with conftest.py
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-voice-agent-backend/03-CONTEXT.md
@.planning/phases/03-voice-agent-backend/03-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs to understand -->

From backend/config.py:
```python
@dataclass
class Config:
    gcp_project_id: str = os.getenv("GCP_PROJECT_ID", "vision-gridai")
    gcp_region: str = os.getenv("GCP_REGION", "europe-west1")
    gemini_model: str = os.getenv("GEMINI_MODEL", "gemini-live-2.5-flash-native-audio")
    google_application_credentials: str = os.getenv("GOOGLE_APPLICATION_CREDENTIALS", "secrets/openclaw-key-google.json")
    supabase_url: str = os.getenv("SUPABASE_URL", "")
    supabase_service_key: str = os.getenv("SUPABASE_SERVICE_KEY", "")

config = Config()
```

From backend/logger.py:
```python
async def log_event(
    event_name: str,
    message: str,
    *,
    component: str = "voice_agent",
    event_type: str = "info",
    lead_id: str | None = None,
    metadata: dict | None = None,
) -> None:
    # Posts to {SUPABASE_URL}/rest/v1/pipeline_logs with Content-Profile: sales_agent
```

From Firestore knowledge_base collection (seeded in Phase 2):
- Document IDs: programmes, conversation-sequence, faqs, payment-details, objection-handling
- Each doc has: content (str), source_file (str), page_count (int), extracted_at (timestamp), version (str), keywords (list)
- Total content ~43K chars across 5 docs

From Supabase sales_agent.leads table columns (relevant to tools):
- id (uuid), name (text), phone (text), email (text), status (text)
- company (text), role (text), experience_level (text), cloud_background (text), motivation (text)
- preferred_programme (text), metadata (jsonb)

From Supabase sales_agent.call_logs table columns (relevant to tools):
- id (uuid), lead_id (uuid FK), status (text), outcome (text)
- transcript (text), qualification_summary (text), recommended_programme (text)
- objections_raised (jsonb), duration_seconds (int), follow_up_date (timestamptz)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Knowledge loader and system instruction builder with tests</name>
  <files>backend/knowledge_loader.py, tests/conftest.py, tests/test_knowledge_loader.py, tests/test_system_instruction.py, backend/requirements.txt</files>
  <behavior>
    - test_kb_preload: Mock Firestore returns 5 docs with content -> load_knowledge_base() returns concatenated string containing all 5 doc sections
    - test_kb_missing_doc: Mock Firestore returns only 4 docs -> load_knowledge_base() still returns the 4 available docs without error
    - test_ai_disclosure: build_system_instruction("Akinwunmi", kb_content) output contains "I'm an AI assistant" and "call is being recorded"
    - test_qualification_fields: System instruction mentions all 4 must-have qualification fields: role, experience_level, cloud_background, motivation
    - test_programme_recommendation: System instruction mentions both "Cloud Security" and "SRE" (or "Platform Engineering")
    - test_objection_handling_rules: System instruction contains reactive objection handling guidance (wait for lead to raise, two attempts)
    - test_name_personalization: build_system_instruction("Akinwunmi", kb_content) output contains "Akinwunmi"
    - test_watchdog_instructions: System instruction contains wrap-up behavior instructions for the 8.5-minute signal
    - test_commitment_rules: System instruction defines COMMITTED, FOLLOW_UP, DECLINED thresholds
    - test_follow_up_timing: System instruction instructs Sarah to ask when the lead prefers a follow-up
  </behavior>
  <action>
    1. Update backend/requirements.txt: Pin google-adk>=1.26.0 (was >=0.3.0), google-genai>=1.66.0 (was >=1.56.0). Add soxr>=0.5.0, pytest>=8.0.0, pytest-asyncio>=0.24.0, pytest-mock>=3.14.0.

    2. Create tests/conftest.py with shared fixtures:
       - mock_firestore_db: Returns a mock AsyncClient with 5 documents in knowledge_base collection. Each doc.to_dict() returns {"content": "mock content for {doc_id}", ...} for: programmes, conversation-sequence, faqs, payment-details, objection-handling.
       - mock_supabase_config: Provides fake supabase_url and supabase_service_key.
       - sample_kb_content: A string with representative KB content including programme names, prices, objection responses.
       - Configure pytest-asyncio with asyncio_mode=auto in a pyproject.toml [tool.pytest.ini_options] section OR pytest.ini.

    3. Create tests/test_knowledge_loader.py testing load_knowledge_base() and build_system_instruction().

    4. Create tests/test_system_instruction.py testing the full system instruction output for CONTEXT.md locked requirements:
       - AI disclosure wording (CALL-02)
       - Qualification field mentions (CALL-03)
       - Programme details presence (CALL-04)
       - Objection handling rules (CALL-05)
       - Commitment ask thresholds (CALL-06)
       - Follow-up timing instruction (CALL-10)
       - Watchdog wrap-up behavior (CALL-08)

    5. Create backend/knowledge_loader.py:
       - load_knowledge_base(db: firestore.AsyncClient) -> str: Fetches all 5 KB_DOCS from Firestore knowledge_base collection. Concatenates with section headers. Returns the combined KB text.
       - build_system_instruction(lead_name: str, kb_content: str) -> str: Builds the COMPLETE system instruction with:
         a. Sarah's persona (enthusiastic coach, Nigerian-aware, standard English, no Pidgin)
         b. AI disclosure opening (locked wording from CONTEXT.md: "Hi [name], this is Sarah from Cloudboosta. Just so you know, I'm an AI assistant and this call is being recorded. I'd love to learn about your cloud career goals -- is now a good time?")
         c. Qualification flow rules (conversational weaving, must-have fields: role, experience_level, cloud_background, motivation, recommend AFTER all fields gathered)
         d. Programme recommendation rules (personalized reasoning, Cloud Security GBP1200, SRE & Platform Engineering GBP1800)
         e. Objection handling rules (reactive only, two attempts with different angles, use salary figures from KB)
         f. Commitment ask rules (natural summary then direct ask, COMMITTED=explicit yes, FOLLOW_UP=ambiguous, DECLINED=explicit no)
         g. Duration watchdog instructions (when [INTERNAL SYSTEM SIGNAL] arrives, wrap up naturally without mentioning timer)
         h. Follow-up timing (ask lead when they prefer a follow-up call for FOLLOW_UP outcomes)
         i. Name usage rules (3-4 times: greeting, mid-call, closing)
         j. Small talk rules (1-2 exchanges then redirect)
         k. The full KB content injected in a [KNOWLEDGE BASE] section
         l. Tool usage instructions (when to call update_lead_profile, when to call determine_call_outcome)

    6. Run tests: pytest tests/test_knowledge_loader.py tests/test_system_instruction.py -x -v
  </action>
  <verify>
    <automated>cd backend && pip install -r requirements.txt -q && cd .. && python -m pytest tests/test_knowledge_loader.py tests/test_system_instruction.py -x -v</automated>
  </verify>
  <done>
    - knowledge_loader.py exports load_knowledge_base() and build_system_instruction()
    - System instruction contains AI disclosure matching CONTEXT.md locked wording (CALL-02)
    - System instruction references all 4 qualification fields (CALL-03)
    - System instruction includes both programmes with prices (CALL-04)
    - System instruction includes objection handling rules from CONTEXT.md (CALL-05)
    - System instruction defines COMMITTED/FOLLOW_UP/DECLINED thresholds (CALL-06)
    - System instruction includes watchdog wrap-up behavior (CALL-08)
    - System instruction includes follow-up timing question (CALL-10)
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ADK function tools with Supabase integration and tests</name>
  <files>backend/tools.py, tests/test_tools.py</files>
  <behavior>
    - test_update_lead_profile_success: Calling update_lead_profile with 4 fields + mock ToolContext stores qualification dict in tool_context.state["qualification"]
    - test_update_lead_profile_returns_success: Return value is {"status": "success", "message": "..."}
    - test_determine_outcome_committed: outcome="COMMITTED" stores correct dict in tool_context.state["call_outcome"]
    - test_determine_outcome_follow_up: outcome="FOLLOW_UP" with follow_up_preference stores the preference
    - test_determine_outcome_declined: outcome="DECLINED" stores correctly
    - test_determine_outcome_invalid: outcome="MAYBE" returns {"status": "error", ...}
    - test_follow_up_preference_captured: FOLLOW_UP outcome with follow_up_preference="Tuesday at 3pm" is stored in state
    - test_supabase_lead_update: write_lead_profile_to_supabase() makes PATCH to correct URL with Content-Profile: sales_agent
    - test_supabase_call_log_write: write_call_log_to_supabase() makes POST to correct URL with Content-Profile: sales_agent
  </behavior>
  <action>
    1. Create tests/test_tools.py testing all tool functions with mock ToolContext and mock httpx.

    2. Rewrite backend/tools.py (replace stub) with:
       a. update_lead_profile(role, experience_level, cloud_background, motivation, tool_context: ToolContext) -> dict:
          - Stores qualification data in tool_context.state["qualification"]
          - Returns {"status": "success", "message": "Lead profile updated"}
          - Docstring follows ADK auto-schema conventions (describes args clearly for Gemini to understand when to call)

       b. determine_call_outcome(outcome, recommended_programme, qualification_summary, objections_raised: list[str], follow_up_preference: str = "", tool_context: ToolContext = None) -> dict:
          - Validates outcome in ("COMMITTED", "FOLLOW_UP", "DECLINED")
          - Returns error dict for invalid outcomes
          - Stores full outcome dict in tool_context.state["call_outcome"]
          - Includes follow_up_preference for FOLLOW_UP outcomes
          - Returns {"status": "success", "outcome": outcome}

       c. write_lead_profile_to_supabase(lead_id: str, profile: dict) -> None:
          - Async helper. PATCH to {supabase_url}/rest/v1/leads?id=eq.{lead_id}
          - Uses Content-Profile: sales_agent header (established pattern)
          - Non-blocking (fire-and-forget from tools, awaited in call cleanup)

       d. write_call_log_to_supabase(lead_id: str, call_data: dict) -> str:
          - Async helper. POST to {supabase_url}/rest/v1/call_logs
          - Uses Content-Profile: sales_agent, Prefer: return=representation
          - Returns the created call_log id
          - call_data includes: lead_id, status, outcome, transcript, qualification_summary, recommended_programme, objections_raised, duration_seconds, follow_up_date

    3. Run tests: pytest tests/test_tools.py -x -v
  </action>
  <verify>
    <automated>python -m pytest tests/test_tools.py -x -v</automated>
  </verify>
  <done>
    - tools.py exports update_lead_profile and determine_call_outcome as ADK-compatible function tools
    - tools.py exports write_lead_profile_to_supabase and write_call_log_to_supabase as async helpers
    - determine_call_outcome validates outcome enum (CALL-06)
    - determine_call_outcome captures follow_up_preference (CALL-10)
    - All Supabase calls use Content-Profile: sales_agent header
    - All tests pass
  </done>
</task>

</tasks>

<verification>
All tests pass: python -m pytest tests/test_knowledge_loader.py tests/test_system_instruction.py tests/test_tools.py -x -v
</verification>

<success_criteria>
- knowledge_loader.py creates a complete system instruction with persona, locked AI disclosure, qualification flow, programme details, objection handling rules, commitment thresholds, watchdog behavior, and full KB content
- tools.py provides ADK-compatible function tools that store state correctly and write to Supabase with the established Content-Profile pattern
- Test suite covers all requirement behaviors (CALL-02 through CALL-06, CALL-08, CALL-10) with automated verification
- requirements.txt updated with correct ADK and genai versions
</success_criteria>

<output>
After completion, create `.planning/phases/03-voice-agent-backend/03-01-SUMMARY.md`
</output>
