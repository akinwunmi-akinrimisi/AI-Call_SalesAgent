---
phase: 01-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - backend/.env.example
  - backend/config.py
  - .gitignore
  - skills.sh
  - scripts/validate_pdfs.py
  - scripts/validate_services.py
  - backend/requirements.txt
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 5 PDFs exist in knowledge-base/ and contain extractable, expected content"
    - "Environment variables are documented correctly in .env.example with correct project ID, model, and auth method"
    - "Service account JSON files live in secrets/ directory, not project root"
    - "All external services (Supabase, n8n, OpenClaw, Resend) are reachable"
    - "Config.py reflects Vertex AI service account auth, not API key auth"
  artifacts:
    - path: "scripts/validate_pdfs.py"
      provides: "PDF extraction and keyword validation for all 5 knowledge base documents"
      min_lines: 60
    - path: "scripts/validate_services.py"
      provides: "Reachability check for Supabase, n8n, OpenClaw, Resend"
      min_lines: 40
    - path: ".env.example"
      provides: "Corrected master environment template"
      contains: "vision-gridai"
    - path: "backend/config.py"
      provides: "Updated config with Vertex AI auth pattern"
      contains: "google_application_credentials"
    - path: "secrets/"
      provides: "Directory for service account JSON files"
  key_links:
    - from: ".env.example"
      to: "backend/config.py"
      via: "environment variable names must match"
      pattern: "GCP_PROJECT_ID|GEMINI_MODEL|GOOGLE_APPLICATION_CREDENTIALS"
    - from: "scripts/validate_pdfs.py"
      to: "knowledge-base/*.pdf"
      via: "PyMuPDF extraction"
      pattern: "fitz\\.open|pymupdf4llm"
    - from: "scripts/validate_services.py"
      to: ".env"
      via: "dotenv loading for service URLs and keys"
      pattern: "load_dotenv|os\\.getenv"
---

<objective>
Prepare the project foundation: fix incorrect configuration values across all env files and config.py, secure service account files in secrets/ directory, copy PDFs into knowledge-base/, install Phase 1 dependencies, and create validation scripts for PDFs and service reachability.

Purpose: Every downstream phase depends on correct env vars, the right GCP project ID, proper auth method, and validated knowledge base content. This plan eliminates all configuration debt before API validation begins.
Output: Corrected config files, secured secrets, populated knowledge-base/, two validation scripts (PDFs + services), updated dependencies.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prerequisites/01-CONTEXT.md
@.planning/phases/01-prerequisites/01-RESEARCH.md
@.planning/phases/01-prerequisites/01-VALIDATION.md

@.env.example
@backend/.env.example
@backend/config.py
@.gitignore
@skills.sh
@backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix configuration files, secure secrets, and install dependencies</name>
  <files>.env.example, backend/.env.example, backend/config.py, .gitignore, skills.sh, backend/requirements.txt</files>
  <action>
1. Update `.env.example` (root):
   - Line 8: Change `GCP_PROJECT_ID=cloudboosta-agent` to `GCP_PROJECT_ID=vision-gridai`
   - Line 10: Change `GEMINI_MODEL=gemini-2.0-flash-live` to `GEMINI_MODEL=gemini-live-2.5-flash-native-audio`
   - Line 11: Remove `GOOGLE_GEMINI_API_KEY=` entirely
   - Add new line after GCP_REGION: `GOOGLE_APPLICATION_CREDENTIALS=secrets/openclaw-key-google.json`
   - Add new line: `GCP_REGION=europe-west1` (keep existing, confirm value)
   - Add `RESEND_API_KEY=` in a new "Resend" section between n8n and OpenClaw sections

2. Update `backend/.env.example`:
   - Line 4: Change `GCP_PROJECT_ID=cloudboosta-agent` to `GCP_PROJECT_ID=vision-gridai`
   - Line 5: Change `GEMINI_MODEL=gemini-2.0-flash-live` to `GEMINI_MODEL=gemini-live-2.5-flash-native-audio`
   - Line 6: Remove `GOOGLE_GEMINI_API_KEY=` line
   - Add `GOOGLE_APPLICATION_CREDENTIALS=secrets/openclaw-key-google.json`
   - Add `GCP_REGION=europe-west1`

3. Update `backend/config.py`:
   - Line 10: Change default from `"cloudboosta-agent"` to `"vision-gridai"`
   - Line 11: Change default from `"gemini-2.0-flash-live"` to `"gemini-live-2.5-flash-native-audio"`
   - Line 12: Replace `google_gemini_api_key` field with `google_application_credentials: str = os.getenv("GOOGLE_APPLICATION_CREDENTIALS", "secrets/openclaw-key-google.json")`
   - Add `gcp_region: str = os.getenv("GCP_REGION", "europe-west1")` after gcp_project_id
   - In `validate()` method: Replace `"GOOGLE_GEMINI_API_KEY": self.google_gemini_api_key` with `"GOOGLE_APPLICATION_CREDENTIALS": self.google_application_credentials`

4. Update `.gitignore`:
   - Add `secrets/` on its own line (before the Python section)
   - Keep existing `*.json` and exceptions
   - Remove the line `openclaw-key-google.json` (no longer needed since secrets/ covers it)

5. Create `secrets/` directory:
   - `mkdir -p secrets`
   - Move `my-n8n-service-account.json` from project root to `secrets/`
   - Move `openclaw-key-google.json` from project root to `secrets/`
   - Verify: `ls secrets/` shows both files

6. Update `skills.sh`:
   - Line 84: Change `gcloud config set project cloudboosta-agent` to `gcloud config set project vision-gridai`
   - Line 161: Change `coming-soon.pdf` to `objection-handling.pdf` in the PDF check loop

7. Update `backend/requirements.txt` -- add Phase 1 validation dependencies:
   - Add: `google-genai>=1.56.0`
   - Add: `google-auth>=2.0.0`
   - Add: `pymupdf>=1.24.0`
   - Add: `pymupdf4llm>=0.0.14`
   - Add: `resend>=2.0.0`
   - Add: `supabase>=2.0.0`
   - Add: `audioop-lts>=0.2.1`
   - Keep all existing entries

8. Install dependencies:
   - Run: `pip install google-genai google-auth pymupdf pymupdf4llm python-dotenv twilio resend supabase httpx audioop-lts`

9. Also update the `.env` file (the real one, not example):
   - Change `GEMINI_MODEL` to `gemini-live-2.5-flash-native-audio`
   - Add `GOOGLE_APPLICATION_CREDENTIALS=secrets/openclaw-key-google.json`
   - Change `GCP_PROJECT_ID` to `vision-gridai` if it currently says `cloudboosta-agent`

IMPORTANT: Do NOT modify AGENT.md (the file says "Do not rewrite"). Only update the files listed above.
  </action>
  <verify>
    <automated>python -c "from backend.config import Config; c = Config(); assert c.gcp_project_id == 'vision-gridai', f'Wrong project: {c.gcp_project_id}'; assert 'native-audio' in c.gemini_model, f'Wrong model: {c.gemini_model}'; assert hasattr(c, 'google_application_credentials'), 'Missing credentials field'; print('Config OK')" && ls secrets/openclaw-key-google.json secrets/my-n8n-service-account.json && grep "vision-gridai" .env.example && grep "objection-handling" skills.sh</automated>
  </verify>
  <done>
    - .env.example, backend/.env.example, backend/config.py all reference vision-gridai, gemini-live-2.5-flash-native-audio, and GOOGLE_APPLICATION_CREDENTIALS
    - secrets/ directory contains both service account JSON files, project root has neither
    - .gitignore includes secrets/
    - skills.sh checks for objection-handling.pdf (not coming-soon.pdf)
    - All Phase 1 Python dependencies installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Copy PDFs and create PDF validation script</name>
  <files>knowledge-base/programmes.pdf, knowledge-base/conversation-sequence.pdf, knowledge-base/faqs.pdf, knowledge-base/payment-details.pdf, knowledge-base/objection-handling.pdf, scripts/validate_pdfs.py</files>
  <action>
1. Ask the user where the 5 PDF files are located on their machine, then copy them into `knowledge-base/`:
   - programmes.pdf
   - conversation-sequence.pdf
   - faqs.pdf
   - payment-details.pdf
   - objection-handling.pdf
   If PDFs are already somewhere in the project tree or known location, copy directly. If not found, create a checkpoint:human-action for the user to place them.

   NOTE: The user confirmed in CONTEXT.md that "All 5 PDFs already exist as .pdf files on this machine, ready to copy into knowledge-base/". Search common locations first: project root, docs/, Downloads folder.

2. Create `scripts/validate_pdfs.py` implementing the Pattern 4 from RESEARCH.md:

```python
"""Validate knowledge base PDFs: existence, extractability, and expected content.

Checks all 5 PDFs in knowledge-base/ directory:
- programmes.pdf: Programme details, pricing, duration
- conversation-sequence.pdf: Qualification decision tree
- faqs.pdf: Common questions and answers
- payment-details.pdf: Bank transfer instructions
- objection-handling.pdf: Objection response scripts

Usage: python scripts/validate_pdfs.py
"""
```

The script must:
- Import pymupdf4llm and fitz (PyMuPDF)
- Define expected keywords per PDF (from RESEARCH.md PDF Section Validation Criteria):
  - programmes.pdf: ["Cloud Security", "SRE", "Platform Engineering", "1,200", "1,800", "duration", "curriculum"]
  - conversation-sequence.pdf: ["qualification", "background", "experience", "recommend", "Cloud Security", "SRE", "objection", "committed", "follow up", "declined"]
  - faqs.pdf: ["FAQ", "question", "internship", "payment", "beginner"]
  - payment-details.pdf: ["bank", "transfer", "account", "payment"]
  - objection-handling.pdf: ["objection", "expensive", "time", "job", "beginner", "guarantee"]
- For each PDF:
  a. Check file exists in knowledge-base/
  b. Open with fitz.open(), get page count
  c. Extract plain text for keyword validation
  d. Extract markdown with pymupdf4llm.to_markdown() for quality check
  e. Report: page count, character count, markdown length, found/missing keywords
- Use case-insensitive matching for keywords (per RESEARCH.md recommendation: "report findings, not hard-fail" for MEDIUM confidence keywords)
- Print clear PASS/WARN/FAIL per PDF:
  - PASS: file exists, extractable, all keywords found
  - WARN: file exists, extractable, some keywords missing (list which)
  - FAIL: file missing or not extractable
- Print overall summary at end
- Exit code 0 if all PDFs at least WARN (exist and extractable), exit code 1 if any FAIL
  </action>
  <verify>
    <automated>python scripts/validate_pdfs.py</automated>
  </verify>
  <done>
    - All 5 PDFs exist in knowledge-base/
    - validate_pdfs.py runs and reports page count, char count, and keyword matches for each PDF
    - Script exits 0 (all PDFs exist and are extractable)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create service reachability validation script</name>
  <files>scripts/validate_services.py</files>
  <action>
Create `scripts/validate_services.py` implementing the service reachability check pattern from RESEARCH.md:

```python
"""Validate external service connectivity and credential format.

Checks reachability of: Supabase, n8n, OpenClaw, Resend.
Also validates .env completeness against .env.example.

Usage: python scripts/validate_services.py
"""
```

The script must:
- Load .env with python-dotenv
- Check Supabase reachability: GET {SUPABASE_URL}/rest/v1/ with apikey header. Accept 200 or 401 as "reachable" (401 means auth works but no table specified).
- Check n8n reachability: GET {N8N_BASE_URL}/healthz. Accept 200.
- Check OpenClaw reachability: GET {OPENCLAW_API_URL} with follow_redirects=True. Accept 200 or 302. Per RESEARCH.md discretion: reachability check only, full setup deferred to Phase 7.
- Check Resend API key format: Must start with "re_". Do NOT make an API call (avoid sending test emails).
- Check Twilio credentials format: TWILIO_ACCOUNT_SID must start with "AC", TWILIO_AUTH_TOKEN must be non-empty. Do NOT call Twilio API here (that is Plan 02's job).
- Check GOOGLE_APPLICATION_CREDENTIALS file exists at the path specified.
- Check GCP_PROJECT_ID is set and not "cloudboosta-agent" (common pitfall from RESEARCH.md).
- Check GEMINI_MODEL is set and contains "native-audio".
- Compare .env against .env.example: report any vars in .env.example that are missing from .env.

Use httpx with 10-second timeout for all HTTP calls. Print clear PASS/FAIL per service. Print overall summary. Exit 0 if all critical services pass (Supabase, Twilio creds format, GCP config), exit 1 if any critical service fails. OpenClaw and n8n are WARN-only (not critical for Phase 1).
  </action>
  <verify>
    <automated>python scripts/validate_services.py</automated>
  </verify>
  <done>
    - validate_services.py runs and reports connectivity status for each service
    - Supabase, Twilio creds format, GCP config, and credentials file all pass
    - Script reports any missing .env vars compared to .env.example
  </done>
</task>

</tasks>

<verification>
Run the full Phase 1 foundation verification:

```bash
# Config correctness
python -c "from backend.config import Config; c = Config(); print(f'Project: {c.gcp_project_id}, Model: {c.gemini_model}, Creds: {c.google_application_credentials}')"

# Secrets secured
ls secrets/openclaw-key-google.json secrets/my-n8n-service-account.json
test ! -f openclaw-key-google.json && echo "Root clean" || echo "FAIL: JSON still in root"

# PDF validation
python scripts/validate_pdfs.py

# Service reachability
python scripts/validate_services.py
```
</verification>

<success_criteria>
- All configuration files reference vision-gridai, gemini-live-2.5-flash-native-audio, and Vertex AI service account auth
- Service account JSON files are in secrets/ (not project root) and secrets/ is in .gitignore
- All 5 PDFs exist in knowledge-base/ with extractable content and expected keyword matches
- Service reachability script confirms Supabase, credential formats, and GCP config are correct
- Both validation scripts produce clear PASS/WARN/FAIL output
</success_criteria>

<output>
After completion, create `.planning/phases/01-prerequisites/01-01-SUMMARY.md`
</output>
